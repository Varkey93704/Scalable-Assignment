apiVersion: v1
kind: Service
metadata:
  name: scalable-assignment-bluegreen-service
spec:
  type: NodePort
  selector:
    app: scalable-assignment
    track: blue
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30081
