apiVersion: v1
kind: Service
metadata:
  name: scalable-assignment-service
spec:
  type: NodePort
  selector:
    app: scalable-assignment
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
